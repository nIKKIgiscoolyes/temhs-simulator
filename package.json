{
  "name": "temhs-simulator",
  "version": "0.4.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@babylonjs/core": "^8.0.0"
  },
  "devDependencies": {
    "@napi-rs/canvas": "^1.0.8",
    "fake-indexeddb": "6.2.2",
    "prettier": "^3.9.6",
    "typescript": "^5.8.0",
    "vite": "^6.1.0",
    "vitest": "^3.0.0"
  }
}
